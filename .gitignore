# 各 skill 的依賴 — 由各自的安裝腳本本機安裝，
# 因為原生二進位無法跨 OS/架構移植。
node_modules/

# Skill 產出物，非原始碼。
.uiux-audit/

.DS_Store

# Lockfile 要提交：鎖定依賴版本，讓每台機器
# 安裝相同內容（例如 uiux-audit/install.sh 下載的
# playwright 版本所對應的瀏覽器 build）。

# 各 skill 的本機設定（絕對路徑因機器而異，且不該進共用 repo）。
# 以 config.example.json 為範本自行建立。
deck-builder/config.json
